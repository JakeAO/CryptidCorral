<UserControl x:Name="userControl" x:Class="AppWPF.Components.OptionsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="180">
    <Grid>
        <DockPanel VerticalAlignment="Bottom" LastChildFill="False">
            <ScrollViewer x:Name="_scrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="_stackPanel" Width="180" VerticalAlignment="Bottom" />
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
