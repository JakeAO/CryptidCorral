<UserControl x:Class="AppWPF.Components.SelectionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="180">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Bottom" VerticalAlignment="Bottom">
        <DockPanel x:Name="dockPanel" VerticalAlignment="Bottom" LastChildFill="False">
            <StackPanel x:Name="_stackPanel" Width="{Binding Width, ElementName=dockPanel}" VerticalAlignment="Bottom" />
        </DockPanel>
    </ScrollViewer>
</UserControl>
